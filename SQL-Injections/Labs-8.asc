Lab: SQL injection UNION attack, finding a column containing text

🎯 Objetivo

Identificar qué columna de las devueltas por la consulta original tiene un tipo de datos compatible con texto (string), para poder usarla para mostrar datos exfiltrados.

- Estrategia: Prueba de Error y Acierto

Una vez que sabemos el número de columnas (en este lab son 3), debemos probar una por una reemplazando el valor NULL por una cadena de texto aleatoria. Si la columna no acepta texto, la base de datos lanzará un error.

- Pasos de la resolución

Para resolver el lab, debemos encontrar la columna que acepte el valor proporcionado por el laboratorio (por ejemplo: 'abcdef').

Paso A: Probar la primera columna ' UNION SELECT 'abcdef', NULL, NULL-- (Resultado: Error de base de datos o página vacía).

Paso B: Probar la segunda columna ' UNION SELECT NULL, 'abcdef', NULL-- (Resultado: ¡Éxito! El texto 'abcdef' aparece en la pantalla).

Paso C: Probar la tercera columna (Opcional para práctica) ' UNION SELECT NULL, NULL, 'abcdef'-- (Resultado: Error).

Conclusión: Solo la columna 2 es apta para extraer datos de tipo texto.

- Explicación Técnica (Tipos de Datos)

Las bases de datos son estrictas. Si la consulta original espera un Número en la columna 1 y tú le envías un Texto mediante un UNION, la consulta fallará por "Data type mismatch" (Incompatibilidad de tipos). El valor NULL se usa en las pruebas porque puede convertirse automáticamente a cualquier tipo.